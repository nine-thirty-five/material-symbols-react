# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

When you make a user-facing change, run `npx changeset` and follow the prompts. It writes a small
markdown file here describing the change and the semver bump; on release these are collected into
the [CHANGELOG](../CHANGELOG.md) and the version is bumped accordingly.
